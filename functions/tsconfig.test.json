{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // Type-check only, so the build's src-only rootDir does not apply here.
    "rootDir": "."
  },
  "include": ["src", "test"],
  "exclude": ["lib", "node_modules"]
}
